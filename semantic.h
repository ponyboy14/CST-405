#include <stdio.h>
#include "symbolTable.h"
#include <string>

bool checkVarDecl(char itemName[50]) {
    if(found(itemName))
}

bool checkVarTypes(char itemName1[50], char itemName2[50]) {
    if(getItemType(found(itemName1)) == getItemType(found(itemName2)))
}